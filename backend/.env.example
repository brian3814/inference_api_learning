# Default HuggingFace model ID or path to custom model
DEFAULT_MODEL=microsoft/DialoGPT-medium

# Directory for custom models
MODELS_DIR=./models

# Default max generation length
MAX_NEW_TOKENS=256

# Device override: cuda, mps, cpu, or auto
DEVICE=auto

# Server settings
HOST=0.0.0.0
PORT=8000
